KODEA BEZEROAK.BEZEROKODEA%TYPE := &kodea;
IZENA BEZEROAK.BEZEROIZENA%TYPE;


--ALDAGAIARI EZIN DA TAULAKO ZUTABEAREN IZEN BERA EMAN

SELECT BEZEROIZENA INTO IZENA FROM BEZEROAK WHERE BEZEROKODEA = KODEA;
DBMS_OUTPUT.PUT_LINE('BEZEROAREN IZENA: '|| UPPER(IZENA));

---------------------

NIREBEZEROA BEZEROAK%ROWTYPE;


SELECT * INTO IZENA FROM BEZEROAK WHERE BEZEROKODEA = KODEA;
DBMS_OUTPUT.PUT_LINE('BEZEROAREN IZENA: '|| UPPER(NIREBEZEROA.BEZERO));
DBMS_OUTPUT.PUT_LINE('BEZEROAREN IZENA: '|| UPPER(NIREBEZERO.));
DBMS_OUTPUT.PUT_LINE('BEZEROAREN IZENA: '|| UPPER(NIREBEZEROA.TELEFONOA));


EXCEPTION
WHEN NO_DATA_FOUND THEN JASDFIOASDF

END;


---------------------

STOCK PRODUKTUAK.STOCKKOPURUA%TYPE;
IGOERA NUMBER(5,2);

SELECT STOCKKOPURUA FROM PRODUKTUAK WHERE PRODUKTUKODEA = 'FR-100';
CASE
	WHEN STOCK < 10 THEN IGOERA := 1.3;
	WHEN STOCK <=40 THEN IGOERA := 1.1;
	WHEN OTHERS THEN IGOERA := 1.05;
END CASE;
UPDATE PRODUKTUAK SET SALMENTAPREZIOA = SALMENTAPREZIOA * IGOERA WHERE PRODUKTUKODEA= 'FR-100'
COMIT;

END;


----------------------

SELECT * FROM BEZEROAK WHERE BEZEROIZENA IN ('Flores Marivi','Aloha');





------------


kodea bezeroak.bezerokodea%type := &bezerokodea;
	ordainketa_maximoa ordainketak.KOPURUA%TYPE;
ORDAINKETA_MINIMOA ORDAINKETA.KOPURUA%TYPE;


SELECT MAX(KOPURUA) INTO ORDAINKETA_MAX


SELECT BATEAN TALDE FUNTZIOAK BADAUDE, BETI BUELTATUKO DU NULL BALIOAREKIN


RAISE_APLICATION_ERROR(-20001,'AHDHASDIJSAD');